import React from "react";
//import { useSelector } from "react-redux";

const Brand = () => {
//const items = useSelector(state => state);
//console.log(items)
return (
    <div>
      <h2>Check console for Redux State</h2>
    </div>
  );
};

export default Brand;